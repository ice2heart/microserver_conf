# secrets
.env
config/copyparty/copyparty.conf
config/gluetun/auth.toml

# runtime state / app data
data/

# bulk media
downloads/

# backups
bkp.tar.gz
*.bak

# legacy dirs (pre-restructure, pending cleanup)
vpn/
minio/
jellyfin/
qbittorrent/
homarr/
glance/
plex/
portainer/
flood/
var/
copyparty/

# homepage runtime files
config/homepage/logs/

# beets-flask bundles runtime state (library/app db) alongside its config
# under the same /config mount -- only the .yaml files are hand-authored
config/beets/beets/library.db
config/beets/beets-flask/beets-flask-sqlite.db
